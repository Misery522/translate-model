# 贡献指南

欢迎提交可复现的问题、小范围修复和经过讨论的功能。先阅读 [开发路线](docs/ROADMAP.md)，
较大的协议或架构变更请先在 Issue 说明目标和兼容性影响。

## 建立环境

Fork 或克隆仓库，然后创建功能分支：

```powershell
git switch -c fix/short-description
uv sync --locked --group dev
uv run python agent.py
```

当前开发基线为 Python 3.14。不要提交虚拟环境、模型权重、真实词库、录音、
缓存、令牌或个人机器配置。使用 GitHub 提供的 `noreply` 提交邮箱可减少公开个人信息。

## 修改与验证

- 尽量保持每个 PR 聚焦一个问题，并补充能证明用户可见行为的回归测试。
- 输入和模型输出均是不可信数据；不得新增任意代码执行或远程模型默认回退。
- 异步工作必须保留会话及请求代次校验，清空后不得恢复旧内容。
- 文档明确区分已实现能力、实验能力和规划能力。
- 依赖改动需要同时更新 `pyproject.toml` 与 `uv.lock`，说明新增依赖的必要性及许可证。

提交前运行：

```powershell
uv run ruff check .
uv run python -m pytest -q -m "not ollama"
uv pip check
git diff --check
```

后台执行单元测试时为测试进程设置 60 秒运行上限；超时视为检查失败并调查原因，
不要自动终止其他 Python 或 Ollama 进程。真实模型测试按 [README](README.md#测试与开发)
显式启用，记录模型标签、版本、硬件和每个测试耗时，不上传用户原文。

涉及模型行为的改动需要提交脱敏样例与结果；缺少真机或某系统的测试环境时，应
如实标注未验证，不能把其他平台测试当成该平台已通过。

## PR 与发布

使用功能分支和 PR；避免直接向 `main` 推送或重写公共历史。
PR 应描述用户遇到的问题、修改后的行为、执行的验证和已知限制。
外部贡献的 CI 不应访问发布凭据或运行在维护者的个人电脑上。

维护者审查依赖、许可证、兼容性和数据处理变更后再发布版本。模型权重和语音包
单独管理，不随源码提交。贡献默认遵循本仓库 Apache-2.0 许可证；提交前请确认
你有权授权该代码或素材，并保留第三方要求的署名。

所有参与者须遵守 [行为准则](CODE_OF_CONDUCT.md)。
