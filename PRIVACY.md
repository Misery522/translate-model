# 隐私说明

本说明涵盖本地 Gradio、私人 API 和会话式网页，不涵盖尚未验证的原生客户端、
语音或手机离线模型。

## 数据流

浏览器把用户输入交给本机 Python 服务，服务将翻译所需内容提交到本机 Ollama，
再把结果返回浏览器。Python 服务只监听 `127.0.0.1`，模型地址仅允许回环地址，
Gradio 分享与分析统计已关闭。

标准模型调用显式关闭环境代理、HTTP 重定向及 LangSmith 追踪；关闭追踪仅作用于
本轮处理，不修改用户的全局环境设置。自行注入的自定义模型代码不受此承诺覆盖。

译境没有云端账号或云端翻译提供商。私人 API 有设备配对凭据，但没有翻译历史数据库。
该设计不等于对整台电脑
做出的“绝不联网”保证：依赖安装、模型下载和第三方程序更新会联网；本机代理、
Ollama、浏览器插件、恶意软件或用户对源码的修改不受译境控制。

## 保存的数据

- 原文、译文、注解和会话状态会在浏览器与服务的运行时内存中出现。
- 点击清空会重置应用界面并阻止旧结果回填，不保证清除操作系统交换文件、
  崩溃转储、浏览器内存或 Ollama 运行时缓存。
- 自定义术语及指定译法保存在本地 `glossary.json`。源码运行默认位于项目 `data`
  目录；wheel 安装默认位于系统当前用户的 `Yijing` 数据目录，也可通过
  `YIJING_DATA_DIR` 指定位置。词库需要用户主动编辑或删除，其中可能有敏感信息；
  项目默认忽略源码目录的真实词库，自定义目录需要用户自行管理。
- 启动诊断包含本机脚本路径、解释器路径、服务地址和模型状态。
  应用不提供原文或译文的历史日志，但外部日志收集器和调试环境可能另行记录数据。
- 私人网页仅在当前页面内存保留最近 20 轮对话。Service Worker 只缓存固定静态
  界面文件，不缓存 API、原文、译文或设备凭据，不在恢复联网后自动重放请求。
- Cookie 配对凭据由浏览器保存（HttpOnly；远程 HTTPS 模式 Secure、SameSite Strict），
  CSRF 值只在页面内存中。原生 Bearer 由客户端承担保护责任，不得写入网址或日志。
  服务端仅在内存保存令牌摘要、设备名称、会话索引及过期时间。
- API 结果默认保留最多 5 分钟；工作会话空闲 30 分钟过期。设备凭据空闲 30 分钟、
  最长 24 小时过期，服务重启需重新配对。退出会请求吊销凭据并清除关联任务。
- 目前不支持录音上传、语音对话、词库跨云同步或数据分析服务。

## 私人跨设备传输

在用户自行配置的私人 HTTPS 部署中，手机输入经加密连接到自己的电脑，再由本机
Ollama 处理。VPN、DNS、证书及反向代理提供方可能处理其服务所需的连接元数据，
不能把私人部署描述为手机与任何第三方服务完全没有通信。配对码不得公开。
反向代理需关闭正文、Cookie、Authorization 等敏感日志；原文不发送给 GitHub。

清空立即移除网页内容并请求取消电脑任务；断网时无法保证撤销已送达，页面会提示
“未确认服务端停止”。取消回收 API 自己的 Python 推理进程，但不会强制终止 Ollama。
私人 API 中已配对设备共享电脑端词库，配对仅限可信设备，不提供陌生用户隔离空间。

## 用户控制

关闭网页、停止 Python 服务和退出 Ollama 可以结束相应处理。清理术语时应先备份
需要保留的词条，再在界面中修改或移除词库文件。卸载源码不等于删除 Ollama 模型。
向他人报告问题时使用脱敏示例，避免分享个人路径、完整终端截图或真实术语库。

## 后续功能

原生手机应用和语音功能上线前，必须更新本说明，交代设备认证、
传输保护、录音时限、模型位置和删除机制。尚未实现的功能不构成当前隐私承诺。
