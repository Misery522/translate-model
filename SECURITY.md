# 安全政策

当前项目处于早期开发阶段，维护者优先处理默认分支和最新公开版本的问题，
暂不承诺长期支持期限或固定响应时限。

## 报告漏洞

请优先使用 GitHub 仓库的 **Security → Advisories → Report a vulnerability**
私密报告入口（仅在仓库启用私密漏洞报告后可见）。若入口不可见，可提交一个
不包含技术细节的 Issue 请求维护者建立私密沟通渠道；不要在公开 Issue 中发布
令牌、敏感原文、个人路径或可直接利用的攻击步骤。

私密报告中请包含受影响提交或版本、最小复现步骤、预期影响，以及使用脱敏数据的
验证方式。请勿对他人的设备或服务进行未授权测试。

## 关注范围

- 输入导致代码或命令执行、任意文件读取或写入。
- 非回环地址绕过、非预期公网暴露或用户内容泄露。
- 浏览器脚本注入、目录遍历、会话之间结果混淆。
- 依赖或构建流程中的凭据泄露与供应链问题。

普通译文不准确、注释解释错误或模型不遵守语气通常属于质量问题；如其同时突破
上述安全边界，请按漏洞报告。模型输出不可信，不能作为执行操作的授权。

## 运行边界

当前版本仅面向可信用户的本地电脑。回环监听不提供对同机其他程序的隔离。
请勿直接将 Gradio 或 Ollama 端口映射到互联网。后续设备认证 API 尚未交付。
详见 [威胁模型](THREAT_MODEL.md)。
