# 使用支持

一般问题请到 [GitHub Issues](https://github.com/Misery522/translate-model/issues) 提交。
安全漏洞请遵循 [安全政策](SECURITY.md)。项目由社区维护，目前没有商业服务等级承诺。

报告问题时提供提交或版本、操作系统、Python 和 Ollama 版本、模型标签、启动方式、
脱敏复现步骤和必要的错误末尾。请勿上传整个 `.venv`、模型、词库或完整环境报告。

## 常见检查

- **界面打开但翻译失败：** 用 `ollama list` 确认当前服务能看到配置的模型；
  确认 Ollama 已启动。安装依赖和下载模型不等于模型服务正在运行。
- **端口被占用：** 使用终端打印的实际 URL；默认会选择空闲端口。设置了
  `TRANSLATOR_PORT` 时，请更换端口或关闭你确认属于旧实例的进程。
- **模型似乎消失：** `ollama ps` 只显示内存中的模型；再检查 `ollama list`。
  查看服务配置后再考虑重新下载，不要直接删除模型目录。
- **手机打不开地址：** 当前版本只在本机提供服务，手机中的 `127.0.0.1` 指向
  手机自身。跨设备 API 和客户端属于后续开发，不建议直接公开当前端口。
- **输入代码没有变成另一种语言的代码：** 代码注解模式保留程序语义，生成目标语言
  注释或外部说明；它不是编程语言转换器，也不会执行输入。
- **出现 `KeyboardInterrupt`：** 表示运行被中断，常见于 Ctrl+C；仅凭该错误不能
  判断依赖损坏。附上中断前的输出，并说明是否主动终止。

提交问题前可运行 README 中的普通测试。模型翻译质量问题请提供最小、非敏感样例，
包含目标语言、风格、领域和任务模式。
