# 当前版本威胁模型

## 系统与信任边界

当前部署是一个可信用户使用本机浏览器、本机 Python/Gradio 和本机 Ollama。
用户文字与模型输出均不可信。LangGraph 只选择固定处理路径，Python 处理器负责
校验和恢复内容。模型不获得 Shell、文件修改或任意网络工具。

保护对象包括原文、译文、自定义术语、电脑上的文件以及用户对运行状态的正确理解。
当前假设操作系统、Python 环境、Ollama 安装和执行的源码可信。

## 当前控制与剩余风险

| 威胁 | 当前措施 | 剩余风险 |
| --- | --- | --- |
| 提示注入要求执行命令 | 白名单路由、结构化结果校验，无执行工具 | 模型仍可能翻译或解释错误 |
| 原文发往远程模型 | 模型 URL 限制为回环地址，界面显示非法配置警告 | 本机代理或被修改的软件可能转发数据 |
| 格式或代码被改变 | 程序保存结构与保护片段，Python 注释副本校验 AST | 语义解释仍需人工核对 |
| 清空后旧结果回填 | 会话请求代次与过期结果丢弃 | 运行时内存不保证安全擦除 |
| 用户内容变成网页脚本 | 使用组件输出与转义，避免把用户内容当可执行代码 | 依赖漏洞仍可能影响渲染 |
| 公开源码泄露用户数据 | 忽略真实词库、环境文件、日志和模型；提交前审查 | 忽略规则不能清除已进入 Git 历史的数据 |
| 请求占用模型资源 | 文本长度校验与单请求推理约束 | 当前不是带配额的多用户服务，本机程序仍能发请求 |

## 当前不提供的保障

回环地址不能隔离同机恶意程序或其他本机用户；当前界面没有多租户认证。
浏览器扩展、截屏、剪贴板、系统交换文件、Ollama 缓存和外部日志收集不受应用控制。
结构化输出与 AST 比较不能证明译文正确，也不能证明输入代码安全可执行。

不要将 Gradio 或 Ollama 直接公开。个人远程访问、手机和宠物功能上线前，需要
独立评估设备认证、Origin 校验、重放防护、超时、限流、音频处理和更新链路。

## 变更要求

新增外部请求、数据持久化、工具权限、文件解析或跨设备协议时，必须在 PR 中说明
信任边界变化并更新本文件及隐私说明。使用脱敏最小样例验证，禁止在 CI 中处理
真实用户原文。当前安全问题按 [SECURITY.md](SECURITY.md) 报告。
