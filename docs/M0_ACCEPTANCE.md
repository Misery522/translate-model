# M0 本地归档验收记录

验收日期：2026-09-07。当前为 `0.1.0a1` 开源基线候选，不代表后续客户端已完成。

## 已执行

- 在独立克隆中导入白名单源码，保留远端初始提交；未改动原开发副本、虚拟环境或模型。
- 使用 `uv.lock` 建立独立 Python 3.14.7 环境。
- 普通测试、Ruff、依赖一致性检查通过；普通测试进程设置 60 秒硬上限。
- 依赖安全审计未发现当次数据库已知漏洞；结果不构成没有漏洞的保证。
- wheel 含完整源码许可证；源码发行包包含测试所需脚本、示例与配置。
- 在临时解包目录验证业务模块导入与普通测试，防止 editable 安装掩盖打包漏件。
- 真实启动本机 Gradio，验证 HTTP 配置、应用名称和队列；结束测试进程后端口可再次绑定。
- 本机模型通过英译中、长中文同语种原样保留、复杂 Markdown、Python 注解四项验收，
  每项进程最长 60 秒。未将用户原文或真实词库用于测试。

模型：`qwen3.5:4b`；本次 `/api/tags` digest：
`2a654d98e6fba55d452b7043684e9b57a947e393bbffa62485a7aac05ee4eefd`。

## 本次发布前修复

- 主翻译、工作流和冒烟入口局部禁用 LangSmith 追踪，不修改用户全局配置。
- Ollama 客户端禁用环境代理和 HTTP 重定向，继续限制本机回环地址。
- wheel 安装的词库使用用户数据目录，不向 site-packages 写入。
- 发布扫描针对 Git 对象快照工作，拒绝模型、私人数据、凭据和不合规文本编码。
  图像与字体的二进制内容仍需人工审查；该工具不保证识别所有秘密。

## 远端归档门禁

- 基线提交 `9acbe50` 已推送，通过 [PR #1](https://github.com/Misery522/translate-model/pull/1)
  进入归档流程；首轮 [GitHub Actions](https://github.com/Misery522/translate-model/actions/runs/34111165333)
  的 12 项检查全部通过：Windows/Linux × Python 3.11–3.14、依赖审计、两平台打包及汇总。
- `main` 已要求 PR 和 GitHub Actions 来源的 `Required checks`，分支必须与 main 保持同步，
  规则适用于管理员；禁止强推、删除，要求线性历史及解决审查对话。
- 当前按单人维护配置，审批人数为 0，不要求 Code Owner 或最后推送者之外的审批，
  避免维护者无法批准自己 PR 的死锁。
- 后续提交仍须通过上述检查；最终合并和发行状态以 PR 与 GitHub Releases 为准，
  不用一次成功记录代替后续提交的测试。

## 仍需人工或后续阶段验收

- 浏览器完整交互与其他机器的真实模型验收不能由本次 HTTP 启动检查替代。
- API、PWA、宠物、语音及 Android 依照 ROADMAP 分阶段实施，不列入本次已完成项。
