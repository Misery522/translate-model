# M2 验收记录

日期：2026-09-10（复验）。版本：0.3.0a1。对应独立私人 API / PWA PR。

## 已完成

- Python 425 项非模型测试通过；覆盖认证、CSRF、限流、队列、幂等、TTL、静态目录白名单。
- 修复首次调度前取消导致运行位无法释放的竞态；旧取消回调不能清理新任务。
- 前端 73 项测试通过：字符计数、迟到结果、断网清理、超时取消、会话过期、XSS 文本显示。
- 202 响应体中断、畸形 JSON、未知任务响应保留停止入口，不自动重投；取消必须确认终态。
- 提交、轮询和取消均核对任务/会话/客户端请求身份；首个合法响应的任务代次固定，
  其他任务的结果或取消状态不能解除本轮等待，也不能显示为本轮译文。
- Python Ruff、依赖完整性检查及 pip-audit 通过；npm audit 无已知漏洞。
- 复验时发现 OpenAPI 开发工具链的 js-yaml 高危拒绝服务漏洞，已定向更新
  @redocly/openapi-core 1.34.20 / js-yaml 4.3.2；未更换主要运行依赖。
- 本机 `qwen3.5:4b` 通过 API 配对、真实翻译、删除会话、退出与推理子进程回收测试。
  HTTP 协议使用 ASGI TestClient，模型调用及监督子进程为真实实现。
- API + 网页静态文件在临时回环端口成功启动，测试端口与测试服务已回收；未停止 Ollama。
- TypeScript、生产构建通过；生成的 OpenAPI 类型与后端协议一致。
- 构建目录包含 React / react-dom / scheduler 的完整许可证文本。

## 未完成，不能以自动测试替代

- 实际浏览器视觉和交互验收：浏览器工具无法核验平台安全策略，未获准访问测试网页。
  未绕过限制，也未把模拟 DOM 测试标作浏览器通过。
- Android / iOS 真机、添加主屏幕、私人 HTTPS / VPN 实网访问仍待用户设备与部署验证。
- 原生桌面宠物、语音、Android 离线模型不属于 M2 已完成能力。

每次发行仍需对应提交的 GitHub 必需检查通过。模型权重、私人词库、录音、凭据和
本机测试日志不纳入提交。原工作区和原虚拟环境保持不变。
